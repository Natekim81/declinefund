# Vercel용 지역균형발전 사업검색 웹페이지 생성 프롬프트

다음 요건을 충족하는 Vercel 배포용 정적 웹 프로젝트를 만들어라.

## 목적
지역균형발전 사업을 카드 형태로 검색·필터링하는 반응형 웹페이지를 구축한다.

## 필수 기능
1. HTML 문서 타이틀과 메인 제목에 `지역균형발전` 또는 `사업검색`을 포함한다.
2. 사업 카드는 다음 필드를 표시한다.
   - 사업명
   - 지역
   - 사업분야
   - 총사업비
3. 검색 및 필터 기능을 제공한다.
   - 사업명 자유검색
   - 지역 필터
   - 사업분야 필터
   - 필터 초기화
4. 검색 결과 상단에 `결과 N건`을 실시간 표시한다.
5. 제공된 카드뉴스 이미지 중 1장 이상을 히어로 또는 콘텐츠 영역에 표시한다.
6. 대한민국 정책브리핑 `korea.kr` 보도자료 최신 자료를 최소 5건 표시한다.
7. korea.kr RSS 서비스는 중단되었으므로, Vercel 서버리스 함수 `/api/press`가 보도자료 목록 페이지를 서버 측에서 읽고 JSON으로 전달하도록 구현한다.
8. 모바일·태블릿·데스크톱 반응형 레이아웃, 접근 가능한 label/alt, 명확한 빈 결과 상태를 제공한다.
9. 외부 프레임워크 없이 HTML·CSS·JavaScript만 사용하고, API 함수는 Vercel Functions 형식으로 작성한다.

## 배포 구조
- `index.html`
- `api/press.js`
- `public/cardnews.jpg`
- `vercel.json`
- `README.md`

## 디자인
공공기관 사이트의 신뢰감과 현대적인 검색 서비스의 간결함을 결합한다. 짙은 녹색을 주색으로, 노란색을 강조색으로 사용하며 카드에는 충분한 여백과 부드러운 그림자를 적용한다.
